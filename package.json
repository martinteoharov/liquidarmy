{
  "name": "vibes-game",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun run typecheck && bun build src/main.ts --outdir dist --target browser",
    "typecheck": "tsc --noEmit",
    "dev": "bun run build && bun run index.ts",
    "start": "bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/p5": "^1.7.6"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "p5": "^2.1.1"
  }
}
